# Mac Whisper Transcriber

A private, local-first transcription workspace for audio and video on macOS.

The application converts common media formats, splits speech into manageable
chunks, distributes transcription across CPU workers, restores the original
order, and exports a clean text transcript.

